# Clustering V2 — Hackathon MVP
# Local-first dependencies. Zero Docker, zero cloud databases.
# Replaces: bertopic, torch, grpcio, azure-*, redis-*, optuna (from legacy pyproject.toml)

# ── Core ML ────────────────────────────────────────────────────────────────────
sentence-transformers>=2.2.0    # Local embeddings (replaces AIFlow gRPC)
bertopic>=0.16.0                # Topic modeling (same as V1, applied per-domain)
umap-learn>=0.5.0               # Dimensionality reduction
hdbscan>=0.8.33                 # Density clustering
scikit-learn>=1.3.0              # ML utilities
faiss-cpu>=1.7.4                 # Dense vector search (replaces AIDB ANN)
rank-bm25>=0.2.2                 # BM25 sparse search (NEW — V1 has no keyword search)

# ── LLM ────────────────────────────────────────────────────────────────────────
openai>=1.12.0                   # Direct LLM calls (replaces AIFlow gRPC proxy)
tiktoken>=0.5.0                  # Token counting

# ── Storage ────────────────────────────────────────────────────────────────────
duckdb>=0.10.0                   # Local analytical DB (replaces AIDB + Azure Blob + Redis)

# ── UI ─────────────────────────────────────────────────────────────────────────
streamlit>=1.30.0                # Interactive demo app (NEW — V1 has no UI)
plotly>=5.18.0                   # Charts and visualizations
pandas>=2.1.0                    # Data manipulation

# ── Document parsing ──────────────────────────────────────────────────────────
pymupdf>=1.23.0                  # PDF text extraction

# ── Utilities ──────────────────────────────────────────────────────────────────
numpy>=1.24.0
